import * as React from "react"
import Container from '../container';

const Summary = () => {
    return (
        <Container>
           <p>Summary</p>
        </Container>
    );
};

export default Summary;
