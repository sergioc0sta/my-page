import styled from '@emotion/styled';

const HeaderBar = styled.div`
    position: fixed;
    top: 0;
    z-index: 1;
    width: 100vw;
    background: var(--backgroundHeader);
`;

export { HeaderBar };
