import * as React from 'react';
import Container from '../container';

const Work = () => {
    return (
        <Container>
            <p>work</p>
        </Container>
    );
};

export default Work;
