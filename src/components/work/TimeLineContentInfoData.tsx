import React from 'react';
import styled from '@emotion/styled';

const TimeLineContentIfoData = styled.span`
    width: calc(50% - 2px);
    display: inline-block;

    @media (max-width: 500px) {
      display: block;
      width:100%;
    }
`;

export default TimeLineContentIfoData;