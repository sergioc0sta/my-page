import * as React from "react"
import Container from '../container';

const Social = () => {
    return (
        <Container>
           <p>Social</p>
        </Container>
    );
};

export default Social;
